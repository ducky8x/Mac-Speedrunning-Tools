# Mac Speedrunning Tools

A combined macOS app for BetterNBB, WindowBackdrop, Better Piechart, and MACrosshair.

## Build

```sh
swift build
```

To create the app bundle:

```sh
./scripts/build-app.sh
```

The bundled app is written to `dist/Mac Speedrunning Tools.app`.
